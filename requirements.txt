# Core dependencies for running Pulumi CAST AI provider
pulumi>=3.0.0,<4.0.0
-e ./sdk/python

# Build dependencies (needed for editable installs)
setuptools>=68.0.0
wheel>=0.41.0

# Basic runtime dependencies
pyyaml>=6.0

# For development tools (linting, formatting, testing), use:
#   pip install -r requirements-dev.txt 