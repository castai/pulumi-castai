"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.SSOConnection = void 0;
const pulumi = __importStar(require("@pulumi/pulumi"));
const utilities = __importStar(require("./utilities"));
class SSOConnection extends pulumi.CustomResource {
    /**
     * Get an existing SSOConnection resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new SSOConnection(name, state, { ...opts, id: id });
    }
    /** @internal */
    static __pulumiType = 'castai:organization:SSOConnection';
    /**
     * Returns true if the given object is an instance of SSOConnection.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SSOConnection.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["aad"] = state?.aad;
            resourceInputs["additionalEmailDomains"] = state?.additionalEmailDomains;
            resourceInputs["emailDomain"] = state?.emailDomain;
            resourceInputs["name"] = state?.name;
            resourceInputs["oidc"] = state?.oidc;
            resourceInputs["okta"] = state?.okta;
            resourceInputs["syncAuthToken"] = state?.syncAuthToken;
            resourceInputs["synchronizeUserGroups"] = state?.synchronizeUserGroups;
        }
        else {
            const args = argsOrState;
            if (args?.emailDomain === undefined && !opts.urn) {
                throw new Error("Missing required property 'emailDomain'");
            }
            resourceInputs["aad"] = args?.aad;
            resourceInputs["additionalEmailDomains"] = args?.additionalEmailDomains;
            resourceInputs["emailDomain"] = args?.emailDomain;
            resourceInputs["name"] = args?.name;
            resourceInputs["oidc"] = args?.oidc;
            resourceInputs["okta"] = args?.okta;
            resourceInputs["synchronizeUserGroups"] = args?.synchronizeUserGroups;
            resourceInputs["syncAuthToken"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["syncAuthToken"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(SSOConnection.__pulumiType, name, resourceInputs, opts);
    }
}
exports.SSOConnection = SSOConnection;
//# sourceMappingURL=ssoconnection.js.map