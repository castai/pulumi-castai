# Test dependencies for CAST AI Pulumi Python tests

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.3.0  # Parallel test execution

# Pulumi SDKs (required for testing)
pulumi>=3.0.0,<4.0.0
# Use local SDK for testing (editable install)
-e ../../../sdk/python/

# Optional: Cloud provider SDKs for more complete tests
# Uncomment if testing with actual cloud resources
# pulumi-gcp>=6.0.0
# pulumi-aws>=5.0.0
# pulumi-azure-native>=2.0.0
